extends index

block content
    h1= title
    p Displaying all emails in mailing list

    if error 
        p Error getting Dynamic content

    else  
    .container
        .row
            .col-9
                .list-group
                    a.list-group-item Total Emails: #{email_list.length}
                    each email in email_list
                        a.list-group-item.list-group-item-action(href='#') #{email.email}

                    else 
                        a.list-group-item There are no emails.

